For this level, type `nc localhost 30000`, then paste the password for the current level.

**What we learned in this level:**

* Use `nc` (Netcat) to connect to a local port.
* Paste the current level's password to authenticate with the service.
