For this level, you need to run `ls`, and there will be a file named `readme`. Then type:

`cat readme`

This level teaches you how to look for files in a directory using `ls` and read them using `cat`.

