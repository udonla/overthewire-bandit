(Okay, I think you understand how logging into a level and using the password works, so I'll skip explaining that.)

For Level 3, type `ls` again. There will be a file with spaces in its name. Just typing `cat` or `./filename` won't work because of the spaces. We'll use quotation marks:

`cat "spaces in this filename"`

What we learned in this level:

* How to read files that have spaces in their names by using quotation marks.

