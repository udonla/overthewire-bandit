For this level, type `ncat --ssl localhost 30001`, then enter the password for the current level.

**What we learned in this level:**

* Use `ncat --ssl` to connect to a service over SSL/TLS.
* Enter the current level's password to authenticate with the service.

