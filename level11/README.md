Do `ls`.

The `data.txt` file uses Base64 encoding.

Decode it with:

`base64 -d data.txt`

### What we learned in this level:

* Use `ls` to list the files in the current directory.
* Base64 is an encoding format, not encryption.
* Use `base64 -d` to decode a Base64-encoded file.

