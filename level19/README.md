To solve this level, start by logging in with `ssh bandit18@bandit.labs.overthewire.org -p 2220 cat readme`.

**What we learned in this level:**

* Run a command immediately after logging in with `ssh`.
* Read a file without starting an interactive shell.
* Pass a command directly to `ssh` to execute it on the remote machine.

